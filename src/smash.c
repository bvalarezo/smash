#include "global.h"
#include "smash.h"

int smash_init()
{
    int retval, smash_status = 0;
    char *in_line;
    char **args;
    do
    {
        //use readline to get line from stdin
        //args = parse the tokens

        //execute

        /* code */
    } while (smash_status);
    
    return retval;
}

int batch_smash_init(int fd)
{
    return 0;
}

